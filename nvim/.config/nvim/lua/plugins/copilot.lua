-- ~/.config/nvim/lua/plugins/copilot.lua (Minimal - Official Plugin Only)
return {
  {
    "github/copilot.vim",
    -- Optional configuration can go inside a config function if needed later
    -- config = function()
    --   vim.g.copilot_filetypes = { markdown = true }
    -- end
  }
}
